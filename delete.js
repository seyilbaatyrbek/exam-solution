/* eslint-disable */

// Use strict is sooo important
'use strict'; 

// Writing to a console
const writeToAConsole = (...arg) => {
  console.log(arg.join(' '));
}

// Call the function with different arguments
writeToAConsole('//', 'My', 'cat', 'is', 'awesome.');

// The end of the file is always should be a new line
